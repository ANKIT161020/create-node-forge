# This file contains example environment variables.
# Copy this to a new file named `.env` and fill in your actual values.
# dotenv-safe will ensure all variables listed here are present in your .env file.

NODE_ENV=development
PORT=5000

# MongoDB Configuration
MONGODB_URI=mongodb://127.0.0.1:27017/{{projectName}}_dev
# For testing, you might use an in-memory DB or a separate test DB
MONGODB_URI_TEST=mongodb://127.0.0.1:27017/{{projectName}}_test

# JWT Secret and Expiration (REQUIRED FOR AUTHENTICATION)
JWT_SECRET=supersecretjwtkey_please_change_this_in_production
JWT_ACCESS_EXPIRATION_MINUTES=30
JWT_REFRESH_EXPIRATION_DAYS=7

# Other app-specific variables
# APP_SECRET_KEY=another_secret_key